# General application details
spring.application.name=persistency

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# Active profile
spring.profiles.active=${ACTIVE_PROFILE}
